'use client';

import LoginPage from '../login/page';

export default function RegisterPage() {
  // Sesuai standar OAuth terpadu, login & register menggunakan flow Google Sign-In yang sama.
  return <LoginPage />;
}
